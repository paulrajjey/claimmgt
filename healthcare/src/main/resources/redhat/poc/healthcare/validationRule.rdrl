package redhat.poc.healthcare;

import java.lang.Number;

rule "validationRule"
	dialect "mvel"
	ruleflow-group "data"
	when
		claim : Claim( type not in ( "MEDCARE", "MEDCAID", "GROUP" ) )
	then
		claim.setErrorType( "Data" );
		System.out.println("Data error");
end
