package redhat.poc.claim;

import java.lang.Number;

rule "specialClaimRule"
	dialect "mvel"
	ruleflow-group "special"
	when
		claim : Claim( claimAmount > 10000.0 )
	then
		ksession.signalEvent("specialClaim", claim, kcontext.getProcessInstance().getId())
end
